import { Manga } from "~/app/_components/types/mangadex";
export default function MangaHoverCard({
  manga,
  showFullDescription,
  setShowFullDescription,
}: {
  manga: Manga;
  showFullDescription: boolean;
  setShowFullDescription: (show: boolean) => void;
}) {
  // Component implementation
}
